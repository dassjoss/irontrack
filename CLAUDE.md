# Instructions for Claude

- Architecture: 4-layer monolith (api, services, repositories, db).
- Testing: Keep `tests/` matching `app/` structure.
- Environment: Python 3.12+ using `uv`.
- Dependencies: FastAPI, SQLAlchemy, Alembic.
